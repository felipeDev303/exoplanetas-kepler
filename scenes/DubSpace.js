// DubSpace.js
// Escena: estética dub techno

export default class DubSpace {
  constructor(renderer) {
    this.renderer = renderer;
  }
  load(options) {
    this.options = options;
  }
  update(dt) {}
}
